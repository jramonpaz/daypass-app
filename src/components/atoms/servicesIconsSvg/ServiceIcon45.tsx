import * as React from 'react';
import Svg, { SvgProps } from 'react-native-svg';

export const ServiceIcon45 = (props: SvgProps) => (
  <Svg
    viewBox="0 0 50 50"
    width={24}
    height={24}
    {...props}
  />
);
